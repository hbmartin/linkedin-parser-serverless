interface:
  display_name: 'Debug LinkedIn Sample PDFs'
  short_description: 'Debug sample LinkedIn PDF extraction'
  default_prompt: 'Use $debug-linkedin-sample-pdfs to inspect the repo-local samples/ directory by default. If I provide a specific PDF path, sample directory, or parser symptom, focus on that instead.'
